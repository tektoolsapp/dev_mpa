{% extends 'templates/app.twig' %}

{% block content %}
    <h1>Test Products</h1>

    <div class="row">
    {% for index, product in products %}

        <div class="col-md-4 col-sm-6">
            Test
        </div>

        {% if(index + 1) % 3 == 0 %}

            </div>

            <div class="row">

        {% endif %}

    {% endfor %}


    </div>

{% endblock %}